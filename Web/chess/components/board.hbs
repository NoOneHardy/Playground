<div class="board">
    {{#each rows}}
        <div class="row">
            {{#each fields}}
                <div class="field">{{value}}</div>
            {{/each}}
        </div>
    {{/each}}
</div>